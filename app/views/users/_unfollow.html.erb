<%= form_with(model: current_user.active_relationships.find_by(followed_id: @user.id),
              html: { method: :delete }, data: { turbo: false }, local: true) do |f| %>
  <%= f.submit "Unfollow", class: "btn btn-primary" %>
<% end %>
