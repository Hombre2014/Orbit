<% if @feed_items.any? %>
  <%= will_paginate @feed_items, params: { controller: :html_pages, action: :home } %>
  <ol class="snapshots">
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items, params: { controller: :html_pages, action: :home } %>
<% end %>
